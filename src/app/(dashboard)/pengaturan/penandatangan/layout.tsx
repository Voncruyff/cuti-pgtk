import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Penandatanganan - Pengaturan",
  description: "Pengaturan pejabat penandatangan dokumen formulir dan laporan cuti PG Trangkil",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
